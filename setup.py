"""Algorithms python package configuration."""

from setuptools import setup

setup(
    name='algos',
    version='1.0.0',
    packages=['algos'],
    include_package_data=True,
    install_requires=[
        'pytest',
    ],
)
